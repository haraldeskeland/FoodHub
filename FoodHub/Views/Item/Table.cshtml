@model ItemsViewModel

<!-- Display the title with the current view name -->
<h1 class="text-4xl font-semibold">List of all food items</h1>

<!-- Render the partial view for the item table, passing the Items from the model -->
<partial name="_ItemTable" model="Model.Items" />
