// Get the API URL from the environment variables or use an empty string as a fallback
const API_URL = process.env.REACT_APP_API_URL || '';

// Export the API URL as the default export
export default API_URL;